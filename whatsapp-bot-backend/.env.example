# The bot now loads environment variables from the repo root `.env.local`.
# Use `../.env.example` as the canonical template and keep a single root env file.
#
# Optional override:
# NJANGI_ENV_FILE=../.env.local
