name: uptime

# Self-owned external LIVENESS monitor for the production deployment.
# Runs on GitHub's scheduler (every 5 min, the cron floor) from outside Vercel,
# so it catches an outage the in-app crons never could (a down app can't report
# itself). On failure the run goes red — GitHub emails the repo admins by
# default — and, if UPTIME_ALERT_WEBHOOK is set, posts to Slack/Discord too.
#
# This probes /api/health in LIVENESS mode (no ?deep) — "is the app serving?".
# It deliberately does NOT check the database: a DB query every 5 min would
# keep Neon awake 24/7 and burn the free-tier compute quota (the 2026-07-21
# outage). Database reachability is covered separately, on a slower cadence,
# by readiness.yml. A DB-down-but-app-up state therefore shows green here and
# red in readiness — which is exactly the signal we want.
#
# This is the always-on baseline. For sub-minute checks, phone/SMS escalation,
# and a public status page, layer a SaaS monitor (UptimeRobot/BetterStack) on
# the same URL — see docs/deployment-guide.md.

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch: # allow a manual "is prod up?" run

permissions:
  contents: read

concurrency:
  group: uptime
  cancel-in-progress: true

jobs:
  healthcheck:
    name: Probe /api/health (liveness)
    runs-on: ubuntu-latest
    timeout-minutes: 5
    env:
      # Override by setting an Actions *variable* named HEALTHCHECK_URL.
      # Must be the LIVENESS URL (no ?deep) so this probe never wakes Neon.
      HEALTHCHECK_URL: ${{ vars.HEALTHCHECK_URL || 'https://njangionchain.com/api/health' }}
    steps:
      - name: Probe health endpoint (liveness)
        id: probe
        run: |
          set -euo pipefail
          echo "Probing $HEALTHCHECK_URL"
          # Retry transient blips (cold start / network) before declaring DOWN:
          # 3 attempts, 10s apart. Only a sustained failure trips the alert.
          attempt=0
          max=3
          while :; do
            attempt=$((attempt+1))
            code=$(curl -sS -o /tmp/health.json -w '%{http_code}' --max-time 20 "$HEALTHCHECK_URL" || echo "000")
            body=$(cat /tmp/health.json 2>/dev/null || echo '')
            echo "attempt $attempt → HTTP $code | $body"
            # Liveness pass requires only HTTP 200 + status:"ok" (no DB check).
            if [ "$code" = "200" ] \
               && printf '%s' "$body" | grep -q '"status":"ok"'; then
              echo "healthy=true" >> "$GITHUB_OUTPUT"
              echo "✅ app is serving"
              exit 0
            fi
            if [ "$attempt" -ge "$max" ]; then
              {
                echo "healthy=false"
                echo "code=$code"
                echo "body=$body"
              } >> "$GITHUB_OUTPUT"
              echo "::error::Liveness check FAILED after $max attempts (last HTTP $code)"
              exit 1
            fi
            sleep 10
          done

      - name: Alert webhook (optional)
        if: failure()
        env:
          WEBHOOK: ${{ secrets.UPTIME_ALERT_WEBHOOK }}
        run: |
          set -euo pipefail
          if [ -z "${WEBHOOK:-}" ]; then
            echo "UPTIME_ALERT_WEBHOOK not set — relying on GitHub's failed-run email."
            exit 0
          fi
          # Slack- and Discord-compatible {"text"|"content"} payload.
          msg="🔴 njangi-on-chain DOWN — $HEALTHCHECK_URL liveness check failed (run ${{ github.run_id }})"
          curl -sS -X POST -H 'Content-Type: application/json' \
            --data "{\"text\":\"$msg\",\"content\":\"$msg\"}" \
            "$WEBHOOK" || echo "webhook post failed (non-fatal)"
